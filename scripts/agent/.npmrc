# Supply-chain hardening: no npm lifecycle script runs on install. The
# @jeswr/solid-agent-card git dependency commits its built dist/ precisely so it
# is installable with scripts disabled (its `prepare` build is skipped).
ignore-scripts=true
package-lock=true
